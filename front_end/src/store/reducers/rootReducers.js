const initialState = {
    users: [
        {id : 1, name : 'John'},
        {id : 2, name : 'Long'}
    ],
}
const rootReducers = (state = initialState, action) => {
    return state
}

export default rootReducers